{
  "name": "replace-solo",
  "version": "1.2.4",
  "description": "〜ブラウザ内で処理が完結するMicrosoft Loop専用の置換ツール〜",
  "scripts": {
    "test": "npx playwright test projects/app/tests --output projects/app/test-results",
    "build": "python3 scripts/check_version.py",
    "version:bump": "python3 scripts/bump_version.py"
  },
  "author": "Masanori SATAKE",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
