{
  "name": "replace-solo",
  "version": "0.11.0",
  "description": "〜ブラウザ内で処理が完結する置換ツール〜",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir -p releases && python3 scripts/check_version.py && cd projects/app && zip -r ../../releases/Replace-Solo-Chrome.zip . -x \"*.log\"",
    "version:bump": "python3 scripts/bump_version.py"
  },
  "author": "Masanori SATAKE",
  "license": "MIT",
  "devDependencies": {
    "playwright": "^1.59.1"
  }
}
